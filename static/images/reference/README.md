These images are placeholders for the HW10/HW11 prototype only.
Before public launch, replace with:
- Self-captured video stills, OR
- Properly licensed stock imagery
Current sources may not be cleared for commercial use.

To re-tune circle positions, run the app and visit /calibrate (requires app.debug=True or ALLOW_CALIBRATE=1). Drag the circles to the correct joints — JSON updates live.
